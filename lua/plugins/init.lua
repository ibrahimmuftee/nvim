-- Simple plugins that need no config can be listed here.
-- If a plugin needs a config or an opts section, I prefer to split it out
-- into its own file.

return {
    -- Detect tabstop and shiftwidth automatically
    "tpope/vim-sleuth",
}
